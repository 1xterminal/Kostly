// Kostly mobile — basic smoke test
// More specific tests will be added per feature.
import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('placeholder smoke test', (WidgetTester tester) async {
    // TODO: add real widget tests as screens are built
    expect(true, isTrue);
  });
}
